# AImini v0.2.0 发布说明

这是 AImini 从 `v0.1.0` 升级后的功能版本。相比上一个公开版本，本次重点加入了内置本地模型加载能力、视觉模型 `mmproj` 支持、模型加载日志、缓存记录管理，以及更完整的 Markdown/公式渲染体验。

## 推荐版本号

**v0.2.0**

本次更新不是简单修复，而是新增了一条完整的本地模型运行路径：AImini 可以内置 CPU 版 llama.cpp 后端，不再必须依赖 LM Studio 才能加载本地 GGUF 模型。因此建议从 `0.1.0` 升级到 `0.2.0`。

## 主要更新

- 新增内置 Windows x64 CPU 版 `llama-server`。
- 新增“内置模型加载器”，可直接选择本地 `.gguf` 模型文件加载。
- 新增可选 `mmproj` 视觉投影文件配置，用于本地视觉模型的截图/图像输入。
- 新增“加载模型”按钮，实时显示模型加载状态和 llama-server 日志。
- 自动处理中文模型目录：启动时会转换为运行时安全路径，避免 llama.cpp 无法打开中文路径。
- 优化内置 llama-server 单用户桌面场景参数：
  - `--parallel 1`
  - `--no-cont-batching`
  - `--threads-batch`
- 新增对话上下文，让后续提问可以参考最近历史。
- 新增 Markdown 和 KaTeX 数学公式渲染。
- 新增聊天缓存记录、截图缓存、缓存查看器和截图预览窗口。
- 新增缓存目录选择、打开缓存目录、缓存清理策略等设置。
- 设置页左侧导航固定，右侧内容独立滚动。

## 本地模型说明

使用“内置模型加载器”时：

1. 选择 `.gguf` 模型路径。
2. 如果是视觉模型，需要同时选择匹配的 `mmproj*.gguf` 文件。
3. 设置上下文窗口、CPU 线程数和 temperature。
4. 点击“加载模型”，等待状态显示“模型已加载，服务已就绪”。

如果没有配置 `mmproj`，纯文本模型可以正常使用，但截图/图像输入不可用。

## 注意事项

- 当前内置后端是 CPU 版，视觉模型在 CPU 上可能比 LM Studio 或 GPU 后端慢。
- 模型文件不会打包进 AImini，用户需要自行下载并选择 `.gguf` 文件。
- 建议 `.gguf` 文件名使用英文/ASCII。中文目录已做兼容处理，但中文文件名仍建议避免。
- 使用视觉模型时，`mmproj` 必须与模型匹配，否则可能加载失败或无法理解图片。
- 当前 portable exe 未配置自定义应用图标，也没有代码签名证书。

## 发布文件

请将以下文件上传到 GitHub Releases：

```text
dist/AImini-0.2.0-portable.exe
```

SHA256：

```text
C044E9BB42B0117B5814F89EFFBFFD00B9298A7235D70C7BC394125C5BC1CFAD
```

## 从 v0.1.0 升级

1. 下载 `AImini-0.2.0-portable.exe`。
2. 运行 AImini。
3. 右键托盘图标进入设置。
4. 选择 LM Studio、在线 API，或新的“内置模型加载器”。
5. 如果使用本地视觉模型，请同时选择 `.gguf` 和匹配的 `mmproj` 文件。

"""Compatibility entry point for the AImini project.

The desktop app is implemented with Electron because it needs a Windows tray
icon, an always-on-top floating window, and screen capture integration.

Run it with:
    npm.cmd install
    npm.cmd start
"""

print("AImini is an Electron desktop app. Run `npm.cmd install` and `npm.cmd start`.")
